export type Ride = {
  id: string,
  destination: string,
  departureMonth: number,
  departureDay: number,
  departureYear: number,
  returnMonth: number,
  returnDay: number,
  returnYear: number,
  description: string,
  service: string,
  cash: number

}
